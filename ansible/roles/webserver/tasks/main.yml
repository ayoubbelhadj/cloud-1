---
- name: Build and start web services (nginx, wordpress, phpmyadmin, redis)
  ansible.builtin.command: docker compose up -d --build nginx wordpress phpmyadmin redis
  args:
    chdir: "{{ app_dir }}/srcs"
